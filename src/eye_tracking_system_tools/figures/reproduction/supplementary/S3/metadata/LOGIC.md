Same collector as Fig 2f (one point per concurrent pair; unpaired keep the contra-window sample), all saccades, still vs moving with a shared colorbar.
Replot reads figure_S3.pickle; it does not re-detect saccades.

To redraw without the PETS package:
  python replot.py
PDFs land in plots/replot/ (originals in plots/ are not overwritten).
