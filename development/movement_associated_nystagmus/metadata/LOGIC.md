Reviewer Q6 simplified. A large unique gaze event (P75 net_angular_disp, floored at 5°) has a reverse follow-up if another detected saccade, not a binocular constituent, starts within 100 ms with circular direction difference > 90°. Head stationary vs moving is lizMov.mat: any movement sample overlapping the primary on–off span. Rate is compared between those two classes.
Replot reads movement_associated_nystagmus.pkl.

To redraw without the PETS package:
  python replot.py
PDFs land in plots/replot/ (originals in plots/ are not overwritten).
  python replot.py --overwrite   # replace plots/ in place
