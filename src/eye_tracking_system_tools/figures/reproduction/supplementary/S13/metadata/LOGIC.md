Supplementary S13: Fig 2e-style per-animal mean peak speed vs amplitude. Amplitude is onset→offset except detector length-1 events (pre-onset→offset). Identity-ridge points are kept. Panels: all / concurrent / monocular means, gray scatter, paired t-test of per-animal slopes, and animal-colour legend.
Replot reads s13_preonset_2e.pkl and writes S13a–f PDFs.

To redraw without the PETS package:
  python replot.py
PDFs land in plots/replot/ (originals in plots/ are not overwritten).
  python replot.py --overwrite   # replace plots/ in place
