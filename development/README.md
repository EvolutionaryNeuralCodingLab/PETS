# Development / agent workspace

This folder holds **Cursor-agent and internal development artifacts** — implementation plans, interview prompts, scratch scripts, and handoff notes. These files are not part of the shipped product; end users should use `README.md` and `SETUP_INSTRUCTIONS.md` at the repo root.

## Layout

| Path | Contents |
|------|----------|
| `plans/` | Locked implementation specs (preprocessing GUI, block annotator, event explorer) |
| `prompts/` | Agent context prompts and session handoffs |
| `scratch/` | One-off probes, reference prototypes (`GUI_example.py`), debug helpers |

## Active work

**Preprocessing GUI** — resume from `prompts/PREPROCESSING_GUI_HANDOFF.md` and follow `plans/PREPROCESSING_GUI_AGENT_PLAN.md`.
