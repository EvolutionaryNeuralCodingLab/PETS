S8j lizard vs mouse all-saccade 2f. Independent XY limits so detector floors (0.8 vs 3.23 deg/frame) occupy the same axis fraction. Joint and separate colorbar versions. Turbo with zero mapped to white.
Replot reads s8_2f_lizard_mouse_all.pkl.

To redraw without the PETS package:
  python replot.py
PDFs land in plots/replot/ (originals in plots/ are not overwritten).
  python replot.py --overwrite   # replace plots/ in place
