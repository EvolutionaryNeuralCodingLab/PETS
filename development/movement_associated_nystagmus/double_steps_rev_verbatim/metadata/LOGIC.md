Reviewer Q6 verbatim sequence. A large unique saccade is followed within 150 ms by ≥2 smaller detected extras, with ≥1 counter-directed vs the primary (>90°) and ≥1 consecutive pair of extras that reverse relative to each other (back-and-forth). Head still vs moving is lizMov overlap with the primary on–off span.
Replot reads double_steps_rev_verbatim.pkl.

To redraw without the PETS package:
  python replot.py
PDFs land in plots/replot/ (originals in plots/ are not overwritten).
  python replot.py --overwrite   # replace plots/ in place
