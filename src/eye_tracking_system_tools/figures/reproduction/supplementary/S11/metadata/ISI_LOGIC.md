Same-epoch ISIs only (drop intervals that cross a behavior-state boundary). Log and linear histograms for active and quiet.

To redraw without the PETS package:
  python replot.py
PDFs land in plots/replot/ (originals in plots/ are not overwritten).
